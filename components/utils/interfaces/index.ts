export interface IconProps {
  width?: string | number;
  height?: string | number;
  color?: string;
}

export interface IYoutubeChannel {
  id: string;
  channel_name: string;
  joined: string;
  subscribers: string;
  views: string;
}
