query GET_GOOGLE_PROFILES($data: GetGoogleInput!) {
  getLinkedinProfiles(data: $data) {
    totalCount
    profiles {
      id
      company
      rating
      category
      country
      claim_status
      total_reviews
    }
  }
}
